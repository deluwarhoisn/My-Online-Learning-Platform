import React from 'react';
import Navbar from '../Header/Navbar';

const Courses = () => {
    return (
        <div>
           <Navbar></Navbar>
           courses
        </div>
    );
};

export default Courses;